# 📢 CẬP NHẬT TIẾN ĐỘ DỰ ÁN & PHÂN CÔNG BÁO CÁO (Đọc ngay!)

Chào mọi người,
Tối nay mình (Thắng) đã thực hiện merge code và tối ưu hóa project để kịp tiến độ. Dưới đây là các thay đổi quan trọng:

### 1. Code Status
* **SVM & Random Forest:** Đã merge bài của mọi người. Mình có fix nhẹ đường dẫn file data để chạy mượt trên mọi máy. Code gốc vẫn được giữ nguyên.
* **Linear Regression:** Do chưa thấy code trên repo, mình đã **code bổ sung (Baseline)** để đảm bảo bài có đủ các mô hình so sánh.
* **Deep Learning (MLP):** Mình đã thay thế RNN bằng **MLP (Neural Network)** cho phần nâng cao, vì dữ liệu dạng bảng dùng MLP hiệu quả và đúng lý thuyết hơn.
* **Kết quả:** Đã có file `Team_Leader_Advanced_Analysis.ipynb` chạy full luồng: Tuning, Ensemble Learning và vẽ biểu đồ so sánh.

### 2. Phân công viết Báo cáo (LaTeX) - Deadline: [Điền giờ]
Mình đã tạo sẵn file khung `report_structure.tex`. Mọi người vào đó điền nội dung theo phân công:

* **@Thành_viên_A & B:** Viết chương **Data Analysis** và **Methodology** (phần SVM, Random Forest). Giải thích tham số các bạn đã dùng.
* **@Thành_viên_C:** Viết phần **Introduction** và tìm tài liệu tham khảo.
* **@Thắng (Leader):** Mình sẽ viết phần **Deep Learning (MLP)**, **Ensemble Learning**, và **Kết quả thực nghiệm (Evaluation)**.
